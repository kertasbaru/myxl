type: object
properties:
  success:
    type: boolean
    example: true
  message:
    type: string
    example: "Requested Successfully"
  data:
    type: object
    description: Payload data yang dinamis (bisa object atau array)
